#!/bin/bash

cwd="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

echo 'configuring terminal...'

echo 'WARNING: Currently cannot configure terminal profile from command line'
echo 'WARNING: Open application and go Preferences>Profiles>(Gear Icon)>Import'

echo 'completed terminal configure'
